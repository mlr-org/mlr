#' @param facet.wrap.nrow,facet.wrap.ncol [\code{integer()}]\cr
#'   Number of rows and columns for facetting. Default for both is \code{NULL}.
#'   In this case ggplot's \code{facet_wrap} will choose the layout itself. 

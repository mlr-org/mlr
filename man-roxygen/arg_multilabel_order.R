#' @param order ([character])\cr
#'  Specifies the chain order using the names of the target labels.
#'  E.g. for `m` target labels, this must be (`character(1)`) character vector of length `m` that contains (`character(1)`) permutation of the target label names.
#'  Default is `NULL` which uses (`character(1)`) random ordering of the target label names.
#'
